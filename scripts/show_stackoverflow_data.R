#!/usr/bin/Rscript
# read in popular langs from stackoverflow.com, early 2013
#setwd("~/code/svn/eclipse38_dynlang/R_one-offs/R_text_mining/data_public")
#df = read.csv("stackoverflow_proglang_tag_count.txt", sep=" ")
#head(df$langName, 10)
